rootProject {
    buildscript {
        dependencies {
            classpath fileTree(dir: "${initscript.sourceFile.parentFile}/lib", include: '*.jar')
            classpath gradleApi()
            classpath localGroovy()
        }
    }
}
