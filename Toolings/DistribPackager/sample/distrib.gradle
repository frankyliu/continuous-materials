apply from: 'build/build.gradle'

task assemble(type: Zip, overwrite:true) {
    into('share/java') {
        from configurations.distLib
    }
    from '/src/main/resources'
    from 'build/generated'
}
